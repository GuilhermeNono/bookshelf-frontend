export default class Author {
  constructor(email, active, createdAt, updatedAt) {
    this.email = email;
    this.active = active;
    this.createdAt = createdAt;
    this.updatedAt = updatedAt;
  }
}
